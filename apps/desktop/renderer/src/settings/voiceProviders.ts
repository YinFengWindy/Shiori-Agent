/** Display names for the speech services the app can talk to; values stay the config ids. */
export const asrProviderOptions = [{ value: "tencent", label: "腾讯云" }] as const;

/** Display names for the text-to-speech services; values stay the config ids. */
export const ttsProviderOptions = [{ value: "minimax", label: "MiniMax" }] as const;
